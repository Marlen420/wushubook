import swal from 'sweetalert';


export const handleAlertEdit = (title) => {
    swal({ icon: "success", title: title, dangerMode: true })
}

export const handleAlertDelete = (title) => {
    swal({ icon: "success", title: title, dangerMode: true })
}


export const handleAlertPost = (title) => {
    swal({ icon: "success", title: title, dangerMode: true })
}